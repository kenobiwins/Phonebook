export const IconButton = ({ icon }) => {
  return (
    <>
      <IconButton
        variant="outline"
        colorScheme="teal"
        aria-label="Delete contact"
        icon={icon}
      />
    </>
  );
};
